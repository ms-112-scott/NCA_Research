專案結構重組已完成。

新的資料夾結構已建立，相關檔案已移至新位置。此外，`scripts` 資料夾中已建立三個進入點腳本 (`run_lbm.py`, `train_nca.py`, `evaluate_nca.py`)。

您現在即可於 `src/lbm` 目錄下，開始開發您的 Lattice Boltzmann 求解器。

## 關於測試

`tests/` 資料夾中已建立單元測試與整合測試的範例，並新增詳細的 `TESTING.md` 說明文件。

您可以執行 `pytest` 來運行測試，以確保程式碼的正確性。

若需任何協助，請隨時告知。

## 關於檔案位置

gemini 輸出的關於說明或解釋性的md檔案都在 docs\gemini\底下
